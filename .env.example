OPENAI_API_KEY=changeme
OPENAI_PROVIDER=openai
OPENAI_API_BASE=
OPENAI_API_VERSION=
PARLANCHINA_MODELS=gpt-5.1,gpt-5.1-mini
PARLANCHINA_DEFAULT_MODEL=gpt-5.1


LOG_LEVEL=(e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_FORMAT=(e.g., "%(asctime)s %(levelname)s %(name)s: %(message)s")
LOG_TYPE=("stream" for console, "file" for file logging)
LOG_FILE=(filename if using file logging, default: parlanchina.log)